<?xml version="1.0" encoding="utf-8"?>
<!--
  PRODUCTION network security policy.

  SECURITY: base-config denies cleartext for every domain and trusts ONLY the
  system CA store. Previously this file permitted cleartext app-wide and also
  trusted user-installed CAs, which allowed a proxy or malware-planted root
  certificate to transparently intercept auth tokens and financial data.

  Cleartext is permitted ONLY for the loopback/emulator hosts below, which are
  unreachable from a real user's device and exist so local development against a
  dev server keeps working. No LAN IP is listed: a hardcoded developer LAN
  address must never ship in a release build.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
